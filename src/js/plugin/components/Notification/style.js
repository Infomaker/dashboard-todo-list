import styled from 'styled-components'
import { GUI } from "Dashboard";

export const CustomWrapper = styled(GUI.Wrapper)`
    padding-top: 20px;
`

export const ComponentButton = styled(GUI.Button)`
    margin-right: 50px;
    margin-top:20px;
`